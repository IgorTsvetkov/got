<?php
namespace app\helpers;

use app\models\User;
use app\models\Player;

class SpiderEventHelper{
    // public function countHome($player_id)
    // {
    //     Player::find()->where(["id"=>$player_id])->joinWith("propertyGameStatus.rentState")
    // }
}